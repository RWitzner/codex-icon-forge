Create a single {target_kind} for the `{entity_id}` pack representing `{state}`.

Concept: {purpose}.
Style contract: {style_notes}

Output one centered iconic shape on a perfectly flat pure {chroma_key_name} {chroma_key_hex} chroma-key background, sized for a {cell_width}x{cell_height} sticker cell. Keep the silhouette readable at emoji thumbnail size. Frames in this pack are independent — do not try to match identity with other stickers.{state_requirement_text}

Forbidden artifacts:
{transparency_artifact_text}