Create the {target_kind} for the `{entity_id}` design representing `{state}`.

Concept: {purpose}.
Description: {entity_notes}.
Style contract: {style_notes}

Output one centered iconic design on a perfectly flat pure {chroma_key_name} {chroma_key_hex} chroma-key background, sized for a {cell_width}x{cell_height} canvas. The icon must remain readable when scaled down to 16x16 thumbnail size and stay strong at 1024x1024. No text inside the icon, no UI chrome, no photorealism.{state_requirement_text}

Forbidden artifacts:
{transparency_artifact_text}